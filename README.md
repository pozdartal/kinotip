# Kinotip - Inline-бот для рекомендаций фильмов

Inline-бот для Telegram, который пересылает случайные посты из канала шоу "Титр", отмеченные хештегом #showtitrvibe.

## Возможности

- 🔍 **Inline-режим**: используйте бота из любого чата, начиная вводить его username
- 🎲 **Случайная выборка**: каждый раз показывает случайный пост
- 📱 **Поддержка фото**: бот поддерживает текстовые посты и посты с изображениями
- ⚡ **Быстрая отправка**: пересылает посты одним кликом

## Установка и запуск

### 1. Клонируйте репозиторий
```bash
git clone <ваш_репозиторий>
cd kinotip
```

### 2. Создайте бота через @BotFather
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### 3. Настройте окружение
Создайте файл `.env` на основе `env.example` (в Windows PowerShell):
```powershell
copy env.example .env
```

Или создайте файл `.env` вручную с содержимым:
```
BOT_TOKEN=ваш_токен_от_BotFather
CHANNEL_USERNAME=showtitrvibe
```

Откройте `.env` и вставьте свой токен бота от @BotFather.

### 4. Установите зависимости
```bash
pip install -r requirements.txt
```

### 5. Запустите бота
```bash
python bot.py
```

## Использование

### Для пользователей
1. Откройте любой чат в Telegram
2. Начните вводить `@ваш_username_бота`
3. Выберите фильм из предложенных вариантов
4. Пост будет отправлен в чат

### Команды бота
- `/start` - Начать работу с ботом
- `/help` - Показать справку по командам
- `/stats` - Показать статистику добавленных постов
- `/add_post` - Добавить пост в коллекцию (ответьте на сообщение с хештегом #showtitrvibe)

**Важно**: Для добавления поста:
1. Найдите пост с хештегом #showtitrvibe в канале или чате
2. Ответьте на него командой `/add_post`
3. Бот автоматически сохранит пост в коллекцию для рекомендаций

### Ограничения
В текущей версии посты добавляются вручную через команду `/add_post`. Для автоматического получения постов из канала потребуется интеграция с MTProto API (Telethon или Pyrogram).

## Структура проекта

```
kinotip/
├── bot.py              # Основной код бота
├── requirements.txt    # Зависимости Python
├── env.example         # Шаблон файла с настройками
├── .env                # Файл с настройками (создается вручную)
├── .gitignore          # Игнорируемые файлы Git
└── README.md           # Документация
```

## Развитие проекта

### Планируемые улучшения:
- [ ] Интеграция с MTProto API для автоматического получения постов из канала
- [ ] Фильтрация постов по хештегам (#showtitrvibe)
- [ ] Кэширование постов в базе данных
- [ ] Статистика использования бота
- [ ] Админ-панель для управления постами

## Технологии

- [python-telegram-bot 21.x](https://github.com/python-telegram-bot/python-telegram-bot) - Python библиотека для Telegram Bot API
- [python-dotenv](https://github.com/theskumar/python-dotenv) - Управление переменными окружения

## Лицензия

MIT

## Автор

Создано для показа "Титр"

